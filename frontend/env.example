# =============================================================================
# VERCEL (Frontend) — https://adcommedia.in
# =============================================================================

# The browser calls same-origin /api. frontend/vercel.json proxies
# /api/:path* to https://api.adcommedia.in/api/:path*
# Do not set REACT_APP_BACKEND_URL; that would make the browser call the API host directly.
