# =============================================================================
# VERCEL (Frontend) — Environment Variables
# Domain: https://adcommedia.in
# /api/* is rewritten to https://adcom-media-api.onrender.com/api/*
# =============================================================================

# Leave UNSET (or empty) so the browser calls same-origin /api
# Do NOT set this to https://adcom-media-api.onrender.com — that breaks cookies.
# REACT_APP_BACKEND_URL=
